services:
  web:
    container_name: arlovascloud
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    command: ["pnpm", "run", "start"]
